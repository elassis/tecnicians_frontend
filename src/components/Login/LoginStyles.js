import styled from "styled-components";

export const StyledLogin = styled.div`
  > form {
    max-width:500px;
    margin:auto;
    display:flex;
    flex-direction:column;
    gap:10px;

    button {
      width:150px;
      margin:15px auto;
      padding:13px 0;
      font-size:16px;
      font-weight:700;
    }
  }

`;