import styled from "styled-components";

export const StyledModal = styled.div`
  form, fieldset {
    display:flex;
    flex-direction:column;
    gap:10px;

    button{
      padding:12px 10px;
    }
  }
`;