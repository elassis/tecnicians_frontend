import styled from "styled-components";
import Text from "../../../common/components/Text/Text";

export const StyledRejectQuestion = styled.div`
  .confirm_reject_question {
    font-size:16px;
    text-align:left;
    margin:0 0 15px 0;
  }

  .buttons {
    display:flex;
    gap:10px;
  }

  .accepted_text {
    min-width: fit-content;
  }

`;